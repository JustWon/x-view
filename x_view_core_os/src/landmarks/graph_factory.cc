#include <x_view_core/landmarks/graph_factory.h>

namespace x_view {

GraphFactory::GraphFactory(const SemanticGraph& graph) : merged_graph_(graph) {

}

void GraphFactory::merge_graphs(const SemanticGraph& graph) {
//  merged_graph_ = graph;
}

}